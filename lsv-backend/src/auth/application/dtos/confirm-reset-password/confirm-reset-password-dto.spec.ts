import { ConfirmResetPasswordDto } from './confirm-reset-password-dto';

describe('ConfirmResetPasswordDto', () => {
  it('should be defined', () => {
    expect(new ConfirmResetPasswordDto()).toBeDefined();
  });
});
