import { UpdateQuizSubmissionUseCase } from './update-quiz-submission-use-case';

describe('UpdateQuizSubmissionUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateQuizSubmissionUseCase()).toBeDefined();
  });
});
