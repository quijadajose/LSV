import { CreateQuizUseCase } from './create-quiz-use-case';

describe('CreateQuizUseCase', () => {
  it('should be defined', () => {
    expect(new CreateQuizUseCase()).toBeDefined();
  });
});
