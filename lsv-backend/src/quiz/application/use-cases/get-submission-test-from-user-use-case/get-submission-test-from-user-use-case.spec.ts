import { GetSubmissionTestFromUserUseCase } from './get-submission-test-from-user-use-case';

describe('GetSubmissionTestFromUserUseCase', () => {
  it('should be defined', () => {
    expect(new GetSubmissionTestFromUserUseCase()).toBeDefined();
  });
});
