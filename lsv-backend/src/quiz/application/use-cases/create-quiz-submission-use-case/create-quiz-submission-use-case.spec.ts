import { CreateQuizSubmissionUseCase } from './create-quiz-submission-use-case';

describe('CreateQuizSubmissionUseCase', () => {
  it('should be defined', () => {
    expect(new CreateQuizSubmissionUseCase()).toBeDefined();
  });
});
