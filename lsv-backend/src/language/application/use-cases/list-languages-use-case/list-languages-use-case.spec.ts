import { ListLanguagesUseCase } from './list-languages-use-case';

describe('ListLanguagesUseCase', () => {
  it('should be defined', () => {
    expect(new ListLanguagesUseCase()).toBeDefined();
  });
});
