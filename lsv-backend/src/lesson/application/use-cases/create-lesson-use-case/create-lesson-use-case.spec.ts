import { CreateLessonUseCase } from './create-lesson-use-case';

describe('CreateLessonUseCase', () => {
  it('should be defined', () => {
    expect(new CreateLessonUseCase()).toBeDefined();
  });
});
