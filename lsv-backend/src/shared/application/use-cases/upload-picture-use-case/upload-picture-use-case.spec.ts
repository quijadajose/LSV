import { UploadPictureUseCase } from './upload-picture-use-case';

describe('UploadPictureUseCase', () => {
  it('should be defined', () => {
    expect(new UploadPictureUseCase()).toBeDefined();
  });
});
