import { LeaderboardRepositoryInterface } from './leaderboard.repository.interface';

describe('LeaderboardRepositoryInterface', () => {
  it('should be defined', () => {
    expect(new LeaderboardRepositoryInterface()).toBeDefined();
  });
});
