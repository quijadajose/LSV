import { GetStagesFromLanguageUseCase } from './get-stages-from-language-use-case';

describe('GetStagesFromLanguageUseCase', () => {
  it('should be defined', () => {
    expect(new GetStagesFromLanguageUseCase()).toBeDefined();
  });
});
