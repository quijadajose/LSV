import { GetLessonByIdUseCase } from './get-lesson-by-id-use-case';

describe('GetLessonByIdUseCase', () => {
  it('should be defined', () => {
    expect(new GetLessonByIdUseCase()).toBeDefined();
  });
});
