import { DeleteLanguagesUseCase } from './delete-languages-use-case';

describe('DeleteLanguagesUseCase', () => {
  it('should be defined', () => {
    expect(new DeleteLanguagesUseCase()).toBeDefined();
  });
});
